<Window x:Class="ApplAudition.Views.TrayPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ApplAudition.Controls"
        mc:Ignorable="d"
        Title=""
        Height="230"
        Width="280"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Deactivated="OnWindowDeactivated">

    <Border Background="White"
            BorderBrush="LightGray"
            BorderThickness="1"
            CornerRadius="12"
            Padding="15">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Titre -->
            <TextBlock Grid.Row="0"
                       Text="Appli Audition"
                       FontSize="14"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <!-- Jauge miniature -->
            <controls:DbGauge Grid.Row="1"
                              Value="{Binding CurrentDbA}"
                              Category="{Binding ExposureCategory}"
                              ModeLabel="dB(A)"
                              Height="120"
                              Margin="0,5"/>

            <!-- Boutons d'action -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Ouvrir"
                        Click="OnOpenMainWindow"
                        Padding="12,6"
                        Margin="0,0,10,0"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="12"
                        Cursor="Hand"/>

                <Button Content="Paramètres"
                        Click="OnOpenSettings"
                        Padding="12,6"
                        Background="Transparent"
                        BorderBrush="LightGray"
                        BorderThickness="1"
                        FontSize="12"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
